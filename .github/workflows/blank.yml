name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Re-initialize Clean Android Structure
        run: |
          # 1. Ghi đè settings.gradle.kts
          echo 'rootProject.name = "xiomi-assistant"' > settings.gradle.kts
          echo 'include(":app")' >> settings.gradle.kts

          # 2. Ghi đè build.gradle.kts gốc bằng cú pháp Groovy/Kotlin chuẩn không phụ thuộc libs
          cat << 'EOF' > build.gradle.kts
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath("com.android.tools.build:gradle:8.2.2")
                  classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22")
              }
          }
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          EOF

          # 3. Tạo app/build.gradle.kts
          mkdir -p app
          cat << 'EOF' > app/build.gradle.kts
          plugins {
              id("com.android.application")
              id("org.jetbrains.kotlin.android")
          }

          android {
              namespace = "com.example.xiomiassistant"
              compileSdk = 34

              defaultConfig {
                  applicationId = "com.example.xiomiassistant"
                  minSdk = 24
                  targetSdk = 34
                  versionCode = 1
                  versionName = "1.0"
              }
          }
          EOF

          # 4. Tạo AndroidManifest.xml tối thiểu
          mkdir -p app/src/main
          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application
                  android:label="Xiomi Assistant"
                  android:supportsRtl="true">
              </application>
          </manifest>
          EOF

          # 5. Tạo Gradle Wrapper
          gradle wrapper --gradle-version 8.2.2
          chmod +x gradlew

      - name: Build APK
        run: ./gradlew assembleDebug --no-daemon -x test

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
